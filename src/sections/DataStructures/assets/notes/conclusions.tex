\subsubsection{李超线段树}
添加若干条线段或直线，每次求区间上最优直线的值。每个线段树节点只保留一条直线；
两条直线在当前区间各优一部分时，根据交点保留覆盖一半区间的直线，并把另一条下传。复杂度 $O(n\log n)$。

\subsubsection{Segment Tree Beats}
维护最大值 $m$、严格次大值 $s$、最大值个数 $t$ 与区间和。
区间对 $x$ 取最小时：若 $m\le x$ 直接退出；若 $s<x<m$，只更新最大值；否则递归。
均摊复杂度 $O(\log^2 n)$。
